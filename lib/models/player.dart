class Player {
  String id, uid, name;
  int level;

  Player(
      {required this.level,
      required this.id,
      // required this.aid,
      required this.uid,
      required this.name});

  static void play() {}
}
